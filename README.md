# NearWise

NearWise is organized as a small monorepo for the product apps and shared code.

## Workspaces

- `apps/api` - backend service workspace
- `apps/mobile` - mobile client workspace
- `apps/web` - web client workspace
- `packages/shared` - shared utilities and types

## Scripts

Run workspace commands from the repository root:

```sh
npm test
```
